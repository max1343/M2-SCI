package modele;

public abstract class Action {

	public void move(int x, int y, Agent a){
		a.getEnv().deleteBall(a);
		a.setPosX(a.getPosX() + x);
		a.setPosY(a.getPosY() + y);
		a.getEnv().setBall(a);
	}
	
	
}
